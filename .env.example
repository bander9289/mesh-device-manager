# Mesh Network Credentials
# Copy this file to .env and add your actual credentials
# NEVER commit .env to version control!

# Network Key (128-bit hex string, 32 characters)
# Default: TEST credentials (same as previously hard-coded)
MESH_NET_KEY=78806728531AE9EDC4241E68749219AC

# Application Key (128-bit hex string, 32 characters)
# Default: TEST credentials (same as previously hard-coded)
MESH_APP_KEY=5AC5425AA36136F2513436EA29C358D5

# ⚠️  IMPORTANT: These are TEST/DEMO credentials
# For PRODUCTION builds:
# 1. Get production credentials from your team's password manager
# 2. Create a local .env file (already in .gitignore)
# 3. Or use CI/CD secret management (GitHub Secrets, etc.)

# Usage:
# 1. Copy this file: cp .env.example .env
# 2. Update .env with your actual credentials (for production)
# 3. Run: flutter run --dart-define=MESH_NET_KEY=$MESH_NET_KEY --dart-define=MESH_APP_KEY=$MESH_APP_KEY
